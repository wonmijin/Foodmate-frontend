export interface PostCardType {
    id: number,
    date: string,
    title: string,
    meetingName: string,
    when: string,
    where: string,
    maximum: number,
    current: number,
    imageUrl: string,
    nickname: string,
    participationStatus: boolean,
}
