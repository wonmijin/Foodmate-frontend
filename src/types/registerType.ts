export interface registerType {
  email: string;
  nickname: string;
  password: string;
  image: object;
  food: object;
}

export interface emailType {
  email: string;
}

export interface nicknameType {
  nickname: string;
}
